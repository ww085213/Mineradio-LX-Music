# Mineradio 1.5.7.1 更新说明

## 配置保留修复

- 修复部分低性能电脑、显卡初始化较慢或网络资源加载较慢时，自动启动恢复误清空全部本地配置的问题。
- 自动恢复现在会保留壁纸、歌单、播放设置、界面设置和其他用户数据。
- 增加发布检查，禁止自动启动恢复清空整个 `localStorage`。
- 桌面融合改为 WorkerW 双状态切换：锁定时 Mineradio 固定在真实桌面图标后方并将鼠标交给 Explorer；解锁时恢复完整播放器操作。
- Spotify 公开歌单改为快速导入，歌曲在实际播放时按需匹配可播放源，不再因全量预匹配而长时间卡住。
- Spotify 请求使用独立的系统网络/代理通道，避免被桌面音源的直连路由错误绕开。

## 导入修复

- 修复 Spotify（小绿）公开歌单只导入首批 30 首的问题；合并官方公开页与嵌入页数据，并以完整曲目顺序为准。
- 修复 Spotify 两份公开数据字段格式不同导致重复歌曲的问题。
- 增加 Spotify 公开歌单本地缓存；在线导入成功后，即使临时无法访问 Spotify，也能从 `%APPDATA%\Mineradio\spotify-playlist-cache` 恢复已导入歌单。
- 校验小秋、小芸、小蜗、小枸、小枸概念版、小菇、小绿、小水和小果的标准歌单链接识别。
- 修复“音源：一键导入”按钮错误打开本地文件选择器的问题；现在会读取剪贴板中的音源链接或 APIKEY。
- 保留音源链接导入、本地 JS 文件批量导入、自动启用、重复导入覆盖和 `%APPDATA%\Mineradio` 用户级保存。
- 修复本地文件夹导入没有等待扫描完成的问题。
- 修复安装包内 FFmpeg 位于 `resources/app/bin` 时，本地音频兼容转换找不到程序的问题。

## 可视化与体验

- 延续 1.5.7 后续完成的歌词动画、鼓点、音域回响、Wallpaper Engine 投影和底部频谱调整。
- 底部频谱改为全宽视觉映射，高峰可出现在任意位置，并加入平滑随机峰宽和高条数量变化。
- 修复桌面融合锁定态误隐藏全部 Mineradio 内容、只露出原 Wallpaper Engine 壁纸的问题。
- 停用复制桌面图标的原生透明层，避免图标消失、鼠标失效和持续频闪；真实 `SHELLDLL_DefView` 图标层始终由 Explorer 管理。
- 支持 Mineradio 与 Wallpaper Engine 共用桌面宿主：锁定态显示 Mineradio 且桌面图标可操作，再按全局快捷键即可切换回播放器操作。

## 安装可靠性

- 延续 1.5.7 全新安装缺少 `qrcode` 依赖的修复。
- 安装包生成后会从实际 `win-unpacked` 载荷加载二维码模块并生成 SVG、PNG。
- 安装包生成后会核对平台导入、音源导入、FFmpeg、RePKG、主进程和 preload 等关键文件。
- 安装程序只更新程序文件，继续保留 `%APPDATA%\Mineradio` 中的设置、歌单、壁纸和用户音源。
- 保持 `com.mineradio.desktop`、安装目录标记和旧安装路径识别逻辑，可直接覆盖 1.5.7 及更早版本。
